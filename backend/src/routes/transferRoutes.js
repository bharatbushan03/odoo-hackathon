const { Router } = require('express');
const router = Router();

// Transfer routes placeholder
router.get('/', (req, res) => {
  res.json({ message: 'Transfer routes placeholder' });
});

module.exports = router;