# API Contract

Fill this in FIRST once the problem statement drops, before writing backend logic.
Frontend and backend build against this in parallel.

## Endpoint: [name]

**Method + Path:** `GET /api/...`

**Request:**
```json
{}
```

**Response:**
```json
{}
```

**Notes:** (auth required? pagination? error cases?)

---

(duplicate the block above per endpoint)
